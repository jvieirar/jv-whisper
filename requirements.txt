# Python dependencies for jv-whisper transcription scripts
# Install with: pip install -r requirements.txt
#
# Note: mlx-whisper requires Apple Silicon (M1/M2/M3/M4).
# On Intel Macs, replace mlx-whisper with openai-whisper and update scripts/transcribe.py accordingly.

mlx-whisper>=0.4.3
soundfile>=0.13.1
huggingface_hub>=1.10.2
